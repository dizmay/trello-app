export const selectColumns = (state) => state.columns.columns;

export const selectColumnIsError = (state) => state.columns.isError;

export const selectColumnError = (state) => state.columns.error;

export const selectColumnIsLoading = (state) => state.columns.isLoading;